//
//  Palette.swift
//  Lilith
//
//  Created by Mikhail Bukhrashvili on 03.09.24.
//

import SwiftUI

extension Color {
    static let main = MainTheme()
}

struct MainTheme {
    let background = Color("Background")
    let title = Color("Title")
    let secondaryText = Color("SecondaryText")
    let accent = Color("Accent")
    let button = Color("Button")
    let buttonPressed = Color("Button Pressed")
    let cell = Color("Cell")
    let secondaryPressedText = Color("SecondaryPressedText")
    
    
    
    
    
    let zodiacForeground = Color("Zodiac Foreground")
    let zodiacShadow = Color("Zodiac Shadow")
    let blue = Color("Water Element Foreground")
    let red = Color("Fire Element Foreground")
    let gray = Color("Air Element Foreground")
    let green = Color("Earth Element Foreground")
}
